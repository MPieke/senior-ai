import { useEffect, useState } from "react";
import "./styles.css";
type Result={analysisId:string;riskLevel:string;title:string;summary:string;actionRequirement:string;recommendedActions:{type:string;label:string}[];originalText?:string};type Api={analyze:(text:string)=>Promise<Result>;history?:()=>Promise<Result[]>};
const defaultApi={analyze:async(text:string):Promise<Result>=>{const r=await fetch("http://localhost:8000/v1/analyses",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text})});if(!r.ok)throw Error("I couldn't read this right now. Please try again.");return r.json()},history:async():Promise<Result[]>=>{const r=await fetch("http://localhost:8000/v1/analyses");return r.ok?r.json():[]}};
export function App({api=defaultApi}:{api?:Api}){const [screen,setScreen]=useState<"home"|"paste"|"reading"|"result"|"history">("home"),[text,setText]=useState(""),[result,setResult]=useState<Result|null>(null),[showOriginal,setShowOriginal]=useState(false),[history,setHistory]=useState<Result[]>([]),[notice,setNotice]=useState("");useEffect(()=>{if(screen==="history")api.history?.().then(setHistory)},[screen,api]);async function submit(){setScreen("reading");try{setResult(await api.analyze(text));setScreen("result")}catch(e){setNotice(e instanceof Error?e.message:"Please try again.");setScreen("paste")}}const risk=result?.riskLevel==="red"?"Stop and verify before responding.":result?.riskLevel==="yellow"?"Please check before acting.":"No obvious warning signs.";return <main><header><button className="brand" onClick={()=>setScreen("home")}>What’s this about?</button></header>{screen==="home"&&<section><h1>What’s this about?</h1><p>Choose one to get started.</p><button onClick={()=>setScreen("paste")}>Paste a message</button><button disabled>Take a picture <small>Coming soon</small></button><button disabled>Upload a document <small>Coming soon</small></button></section>}{screen==="paste"&&<section><h1>Paste a message</h1>{notice&&<p role="alert">{notice}</p>}<label>Message<textarea value={text} onChange={e=>setText(e.target.value)} /></label><button disabled={!text.trim()} onClick={submit}>Continue</button><button className="secondary" onClick={()=>setScreen("home")}>Go back</button></section>}{screen==="reading"&&<section aria-live="polite"><h1>I’m reading this…</h1><p>Give me a moment.</p></section>}{screen==="result"&&result&&<section><div className={'risk '+result.riskLevel}><strong>{risk}</strong></div><h1>{result.title}</h1><p>{result.summary}</p><h2>Do I need to do anything?</h2><p>{result.actionRequirement==="verify_before_acting"?"Don’t reply or click a link. Verify independently.":"No action needed."}</p><div>{result.recommendedActions.map(a=><button key={a.type} onClick={()=>setNotice(`This would ${a.label.toLowerCase()}. Nothing was sent outside this app.`)}>{a.label}</button>)}</div>{notice&&<p role="status">{notice}</p>}<button className="secondary" onClick={()=>setShowOriginal(!showOriginal)}>{showOriginal?"Hide original message":"Show original message"}</button>{showOriginal&&<pre>{result.originalText}</pre>}</section>}{screen==="history"&&<section><h1>History</h1>{history.length?history.map(item=><button key={item.analysisId} onClick={()=>{setResult(item);setScreen("result")}}>{item.title}</button>):<p>Nothing saved yet.</p>}</section>}<nav><button onClick={()=>setScreen("home")}>Home</button><button onClick={()=>setScreen("history")}>History</button></nav></main>}
