import { test, expect } from "@playwright/test";
test("scam flow keeps original content collapsed", async ({page}) => { await page.goto("/"); await page.getByRole("button",{name:"Paste a message"}).click(); await page.getByLabel("Message").fill("Your bank is locked. Click http://bad.example now."); await page.getByRole("button",{name:"Continue"}).click(); await expect(page.getByText("Stop and verify before responding.")).toBeVisible(); await expect(page.getByText("Your bank is locked.")).toBeHidden(); await page.getByRole("button",{name:"Show original message"}).click(); await expect(page.getByText("Your bank is locked.")).toBeVisible(); });
